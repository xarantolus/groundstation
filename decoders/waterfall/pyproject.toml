[project]
name = "groundstation-waterfall"
version = "0.1.0"
description = "Streaming IQ waterfall plotter (SatNOGS-style rendering)"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "matplotlib>=3.9",
]

[tool.uv]
package = false
